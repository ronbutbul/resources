apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "checklist-chart-test.fullname" . }}-test-connection"
  labels:
    {{- include "checklist-chart-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "checklist-chart-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
